extends layout

block content
  h1= title
  form(method='get')
    input(type='text' name='search')
    input(type='submit' value='Szukaj')
  
  each item in data
    article 
      h1(class='title')=item.title
      h1(class='date')=item.created
      h1=item.description 
